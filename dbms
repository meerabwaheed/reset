-- Create Database
CREATE DATABASE LibraryDB;

-- Use Database
USE LibraryDB;

-- -- Create Books Table
CREATE TABLE Books (
    Book_ID INT PRIMARY KEY,
    Title VARCHAR(50),
    Author VARCHAR(50),
    Price DECIMAL(12,2)
);

-- -- Create Members Table
CREATE TABLE Members (
    Member_ID INT PRIMARY KEY,
    Name VARCHAR(50),
    Email VARCHAR(50)
);

-- -- Create BorrowRecords Table
CREATE TABLE BorrowRecords (
    Record_ID INT PRIMARY KEY,
    Member_ID INT,
    Book_ID INT,
    Borrow_Date DATE,
    FOREIGN KEY (MemberID) REFERENCES Members(MemberID),
    FOREIGN KEY (BookID) REFERENCES Books(BookID)
);

-- -- Insert Data into Books
INSERT INTO Books VALUES
(4, 'Database Systems', 'Navathe', 1500),
(1, 'C# Programming', 'John Sharp', 2500),
(2, 'JavaScript Guide', 'David Flanagan', 2200);

-- -- Insert Data into Members
-- INSERT INTO Members VALUES
-- (101, 'Ali', 'ali@gmail.com'),
-- (102, 'Sara', 'sara@gmail.com');

-- -- Insert Data into BorrowRecords
-- INSERT INTO BorrowRecords VALUES
-- (1, 101, 2, '2026-05-01', '2026-05-15'),
-- (2, 102, 1, '2026-05-05', NULL);

-- -- Display All Books
-- SELECT * FROM Books;

-- -- Display All Members
-- SELECT * FROM Members;

-- -- Join Query
-- SELECT M.Name, B.Title, BR.BorrowDate
-- FROM BorrowRecords BR
-- JOIN Members M ON BR.MemberID = M.MemberID
-- JOIN Books B ON BR.BookID = B.BookID;

-- -- Update Book Price
-- UPDATE Books
-- SET Price = 2000
-- WHERE BookID = 2;

-- -- Delete a Book
-- DELETE FROM Books
-- WHERE BookID = 3;